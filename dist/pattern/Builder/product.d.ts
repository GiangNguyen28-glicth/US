export declare class FilterProductQuery {
    private page;
    private size;
    private price;
    private categoryId;
    private name;
    constructor($page: number, $size: number, $price: number, $categoryId: string, $name: string);
}
